# Повторный замер — что внедрено, что нет

Дата замера: **05.08.2026, вечер** (база для сравнения — тем же утром).
Проверка по `checklist-vnedreniya-2026-08-05.md`, тем же методом: сырой
`curl`-HTML отдельно, отрендеренный DOM через Playwright отдельно — не на
глаз и не по пересказу.

**Главный вывод раньше построчного разбора.** На сайте был деплой — хэш
JS-бандла сменился (`index-2WEJSy7f.js` → `index-sR2riLUV.js`) **на всех
восьми страницах одновременно**. Но сырой прередер-HTML остался
**побайтово идентичным** на 7 из 8 страниц (кроме главной, +4211 байт) — то
есть изменения ушли в живое приложение, а не в статический снимок, который
видит краулер без выполнения JS. Это не гипотеза, а прямое следствие того,
что мы нашли утром: контент этого сайта во многом рендерится отдельно от
пререндер-версии, и именно поэтому фактическое улучшение (см. токены ниже)
может не долетать до той версии страницы, которую индексирует поиск.

---

## A. Критическое — 1 из 4 сделано, и то не полностью

| Пункт | Было | Сейчас | Статус |
|---|---|---|---|
| `tokens.html`, H1 в DOM | `h1s.length === 0` | `h1s: []` — **по-прежнему пусто** | ❌ не сделано |
| `konstruktor-prilozhenij.html`, H1 меняется | сырой «Конструктор приложений вместо Excel» → рендер «Ваш Excel тормозит и врёт?» | то же самое расхождение, без изменений | ❌ не сделано |
| `excel-to-app.html`, форма только после JS | сырой HTML 298 слов, формы нет | сырой HTML **не изменился ни байтом** (14 064 байт), формы всё ещё нет | ❌ не сделано |
| `tokens.html`, «1 токенов» ×3 | было три раза | **исчезло** — таблица переделана полностью: колонки «Действие / Токенов», плюс добавлены новые строки («Пересчёт сложной таблицы» 5–10, «Импорт прайс-листа» 30–50) и ссылка на исследование ActivTrak | ✅ сделано, и с запасом |

**Важная оговорка по последнему пункту.** Это настоящая содержательная
доработка, не косметика — числа неровные и правдоподобные, добавлен
проверяемый источник. Но H1 на этой же странице как был пустым, так и
остался — контент улучшили, техническую причину находки не тронули.

---

## B. Сквозные технические — не сделано ни одного

| Пункт | Было | Сейчас |
|---|---|---|
| Кнопка без имени (`button.text-slate-500`) | на всех проверенных страницах | **на всех восьми страницах без исключения**, включая главную и информационную систему, которые не проверялись отдельно утром |
| Переполнение шапки 768px | `scrollWidth 1079` / `clientWidth 768` | **те же числа, 1079/768, на всех восьми страницах** — идентично утренним |
| `datePublished`/`dateModified` | пусто на сравнении, агентах, каталоге | **по-прежнему `null, null`** на всех трёх |
| `og:image` переиспользуется | `/og/knowledge-base.png` на сравнении, excel-to-app, агентах, токенах | сравнение/excel-to-app/агенты — **без изменений**; токены — переключились на `/og/home.png`, тоже чужая картинка, просто другая. Не исправлено по существу |
| Ссылка на `/knowledge-base` без слеша | лишний редирект 301 | **везде ведёт на `/knowledge-base.html` напрямую** |

**Одно реально исправлено:** редирект на базу знаний убран везде, где
проверено.

---

## C. Перелинковка — крупное системное улучшение, но неполное

**На сайте появилось общее навигационное меню**, которого не было утром —
это не точечная правка по одной ссылке, а изменение шаблона шапки целиком.
Проверено по фактическим `href` на восьми страницах:

| Рекомендация | Статус |
|---|---|
| 1. Главная → Сравнение | ✅ сделано |
| 2. Главная ↔ ИнфоСистема (двусторонняя) | ⚠️ общая связь через меню есть в обе стороны; **конкретная фраза-мостик в FAQ**, которую рекомендовали, не проверялась отдельно — вероятно, не добавлена, так как это точечная правка текста, а изменилось меню |
| 3. Сравнение ↔ Агенты | ⚠️ **половина**: agent-platforms → сравнение появилась (через меню), сравнение → agent-platforms **всё ещё нет** — единственная страница вне общего меню |
| 4. Excel-to-app ↔ Конструктор | ✅ сделано, взаимно |
| 5. Конструктор → Токены | ✅ сделано (через меню) |
| 6. ИнфоСистема → Каталог | ✅ сделано |
| 7. Каталог ↔ Конструктор | ✅ сделано, взаимно |
| 8. Токены → продуктовая | ✅ сделано, и не одна ссылка, а сразу на все основные разделы |

**7 из 8 — фактически решены**, но одним широким жестом (общее меню), а не
точечными контекстными ссылками, которые мы предлагали (ссылка из
конкретного вопроса FAQ, а не просто «страница технически достижима через
шапку»). `agent-platforms.html` — единственная страница, оставшаяся вне
этого меню, и её связь с сравнением так и осталась однонаправленной.

---

## D. Семантика — не сделано ни одной рекомендации

Проверены прицельно 5 приоритетных фраз из `strategiya-poblochno-2026-08-05.md`:

- «crm система что это такое» на сравнении — **нет**
- «CRM-конструктор» (5-й вопрос FAQ) на сравнении — **нет**
- «информационная система и технологии» на ИнфоСистеме — **нет**
- «информационная система и безопасность» на ИнфоСистеме — **нет**
- «информационная система и платформы» на агентах — **нет**
- «аналог excel» на главной — **нет**
- «аналог excel» на конструкторе — **нет**

Ни одна рекомендованная фраза не добавлена ни на одной из проверенных
страниц.

---

## Итог: что клиент реально сделал

1. **Переделал общую навигацию сайта** — это закрывает 7 из 8 пунктов
   перелинковки одним движением, хотя и грубее, чем предлагали контекстные
   ссылки.
2. **Содержательно доработал таблицу токенов** — починил грамматику,
   добавил новые строки с правдоподобными числами и внешний источник. Это
   не было в нашем списке дословно, но по духу совпадает с рекомендацией
   «доработать содержимое `tokens.html`».
3. **Убрал лишний редирект** на базу знаний.

## Что клиент не тронул вовсе

- Все четыре критические находки про H1 и рендер — **три из четырёх
  по-прежнему актуальны** (форма excel-to-app, H1 токенов, H1 конструктора).
- Общий баг шапки (кнопка без имени, переполнение на 768px) — **не тронут
  нигде**, ни на одной из восьми страниц.
- `datePublished`/`dateModified`, `og:image` — не тронуты.
- Ни одна рекомендованная фраза семантического ядра не вписана.

**Практический вывод для разговора с клиентом:** сделанное — это в основном
UX/навигация и одна содержательная доработка, которые видит живой
посетитель. Технические находки, важные именно для поисковика (H1, форма
в сыром HTML, доступность кнопки), не задеты ни одна. Учитывая, что деплой
был сайт-wide, но именно эти конкретные проблемы пережили его без
изменений — стоит явно спросить, знают ли на стороне разработки, что h1 на
`tokens.html` до сих пор не рендерится, или это выпало из вида за более
заметными правками.
